# Only for the checks and the self-test. The app runs without these.
-r requirements.txt

# Starlette's TestClient now prefers the maintained httpx2 client. Keep this
# test-only dependency explicit so clean installs don't silently fall back to
# the deprecated httpx integration.
httpx2>=2.12,<3
